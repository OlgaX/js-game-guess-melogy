const WELCOME = `welcome`;
const GAME_ARTIST = `game-artist`;
const GAME_GENRE = `game-genre`;
const GAME_RESULTS = `game-results`;
const GAME_OVER = `game-over`;

const screen = {
  WELCOME,
  GAME_ARTIST,
  GAME_GENRE,
  GAME_RESULTS,
  GAME_OVER,
};

export default screen;
