const START = `start`; // templateScreen1
const ARTIST = `artist`; // templateScreen2
const GENRE = `genre`; // templateScreen3
const RESULTS = `results`; // templateScreen4
const END = `end`; // templateScreen5

const templateNames = {
  START,
  ARTIST,
  GENRE,
  RESULTS,
  END,
};

export default templateNames;
